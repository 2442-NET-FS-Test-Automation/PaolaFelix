namespace ecommerce.Data.Entities;

// Represents the priority assigned to an order.
public enum Priority
{
    Normal = 0,  // Standard processing.
    Expedited = 1  // Higher priority that gets processed first.
}